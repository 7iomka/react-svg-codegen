<%~ includeFile('./import.eta', it) %>

<% it.generateTypes ?
`type Icons =
   | ${it.names.map(name => `'${name}'`).join('\n  | ')}
` : ''
%>

const Icon = <%~ it.iconsDeclaration %>


export { Icon }
<% it.generateTypes ?
`export type { Icons }`
: ''
%>
